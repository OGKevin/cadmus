name: GitHub Pages docs redirect

on:
  push:
    branches:
      - main
      - master
    paths:
      - .github/gh-pages-redirect/**
      - .github/workflows/cadmus-docs-gh-pages-redirect.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages-docs-redirect
  cancel-in-progress: true

jobs:
  deploy:
    if: ${{ !github.event.repository.fork }}
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Upload GitHub Pages redirect stub
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: .github/gh-pages-redirect
      - name: Deploy redirect to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346 # v5.0.1
